@{
    ViewBag.Title = "Template";
}

<div id="templates-page">
    <div class="d-flex flex-wrap justify-content-between align-items-center mb-4 gap-2">
        <div>
            <h4 class="fw-bold mb-1">Template Checklist</h4>
            <p class="text-muted mb-0">Kelola item checklist audit</p>
        </div>
        <button class="btn btn-primary" data-bind="click: showAddForm">
            <i class="bx bx-plus me-1"></i> Tambah Item
        </button>
    </div>

    <!-- Add/Edit Form -->
    <div class="card mb-4" data-bind="visible: showForm">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0" data-bind="text: editingId() ? 'Edit Template' : 'Tambah Template'"></h5>
            <button type="button" class="btn-close" data-bind="click: cancelForm"></button>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-4">
                    <label class="form-label">Kategori</label>
                    <input type="text" class="form-control" data-bind="value: form.category" placeholder="Contoh: Kebersihan Umum" />
                </div>
                <div class="col-md-4">
                    <label class="form-label">Nama Item</label>
                    <input type="text" class="form-control" data-bind="value: form.name" placeholder="Nama item checklist" />
                </div>
                <div class="col-md-2">
                    <label class="form-label">Urutan</label>
                    <input type="number" class="form-control" data-bind="value: form.displayOrder" min="1" />
                </div>
                <div class="col-md-2 d-flex align-items-end pb-1">
                    <div class="form-check form-switch">
                        <input type="checkbox" class="form-check-input" id="reqGas" data-bind="checked: form.requiresGas" />
                        <label class="form-check-label" for="reqGas">Butuh Gas</label>
                    </div>
                </div>
            </div>
            <div class="mt-3">
                <button class="btn btn-primary me-2" data-bind="click: saveTemplate"><i class="bx bx-check me-1"></i> Simpan</button>
                <button class="btn btn-outline-secondary" data-bind="click: cancelForm">Batal</button>
            </div>
        </div>
    </div>

    <!-- Templates Table -->
    <div class="card">
        <div class="card-header"><h5 class="card-title mb-0">Daftar Template</h5></div>
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="cursor:pointer;user-select:none;white-space:nowrap;" data-bind="click: function(){ ps.sortBy('category') }">
                            Kategori <i class="bx ms-1" data-bind="css: { 'bx-sort': ps.sortField() !== 'category', 'bx-sort-up': ps.sortField() === 'category' && ps.sortAsc(), 'bx-sort-down': ps.sortField() === 'category' && !ps.sortAsc() }"></i>
                        </th>
                        <th style="cursor:pointer;user-select:none;white-space:nowrap;" data-bind="click: function(){ ps.sortBy('name') }">
                            Nama Item <i class="bx ms-1" data-bind="css: { 'bx-sort': ps.sortField() !== 'name', 'bx-sort-up': ps.sortField() === 'name' && ps.sortAsc(), 'bx-sort-down': ps.sortField() === 'name' && !ps.sortAsc() }"></i>
                        </th>
                        <th class="text-center" style="cursor:pointer;user-select:none;white-space:nowrap;" data-bind="click: function(){ ps.sortBy('displayOrder') }">
                            Urutan <i class="bx ms-1" data-bind="css: { 'bx-sort': ps.sortField() !== 'displayOrder', 'bx-sort-up': ps.sortField() === 'displayOrder' && ps.sortAsc(), 'bx-sort-down': ps.sortField() === 'displayOrder' && !ps.sortAsc() }"></i>
                        </th>
                        <th class="text-center">Gas</th>
                        <th class="text-center">Aksi</th>
                    </tr>
                </thead>
                <tbody data-bind="foreach: ps.pagedItems">
                    <tr>
                        <td><span class="badge bg-label-primary" data-bind="text: category"></span></td>
                        <td data-bind="text: name"></td>
                        <td class="text-center" data-bind="text: displayOrder"></td>
                        <td class="text-center">
                            <span class="badge" data-bind="text: requiresGas ? 'Ya' : 'Tidak', css: { 'bg-label-info': requiresGas, 'bg-label-secondary': !requiresGas }"></span>
                        </td>
                        <td class="text-center">
                            <button class="btn btn-sm btn-icon btn-outline-primary me-1" data-bind="click: $root.editTemplate" title="Edit">
                                <i class="bx bx-edit-alt"></i>
                            </button>
                            <button class="btn btn-sm btn-icon btn-outline-danger" data-bind="click: $root.deleteTemplate" title="Hapus">
                                <i class="bx bx-trash"></i>
                            </button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="d-flex flex-wrap justify-content-between align-items-center px-3 py-2" style="border-top:1px solid #e9ecef;" data-bind="visible: templates().length > 0">
            <small class="text-muted" data-bind="text: ps.infoText"></small>
            <nav data-bind="visible: ps.totalPages() > 1">
                <ul class="pagination pagination-sm mb-0">
                    <li class="page-item" data-bind="css: { disabled: !ps.hasPrev() }">
                        <a class="page-link" href="javascript:void(0)" data-bind="click: ps.prev">&#8249;</a>
                    </li>
                    <!-- ko foreach: ps.pageNumbers() -->
                    <li class="page-item" data-bind="css: { active: $data === $root.ps.currentPage() }">
                        <a class="page-link" href="javascript:void(0)" data-bind="text: $data, click: $root.ps.goTo"></a>
                    </li>
                    <!-- /ko -->
                    <li class="page-item" data-bind="css: { disabled: !ps.hasNext() }">
                        <a class="page-link" href="javascript:void(0)" data-bind="click: ps.next">&#8250;</a>
                    </li>
                </ul>
            </nav>
        </div>
        <div data-bind="visible: templates().length === 0" class="text-center text-muted py-5">
            <i class="bx bx-list-check bx-lg d-block mb-2"></i>
            Belum ada template.
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/knockout")
    <script src="~/Scripts/app/templates.viewmodel.js"></script>
    <script>ko.applyBindings(new TemplatesViewModel(), document.getElementById('templates-page'));</script>
}
